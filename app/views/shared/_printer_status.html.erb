<% status = printer_status_label(printer) %>
<span class="status-dot <%= printer_status_dot_class(printer) %> me-1"></span>
<% if status.in?(%w[idle unavailable]) %>
  <span class="text-secondary"><%= status %></span>
<% else %>
  <%= status %>
<% end %>
